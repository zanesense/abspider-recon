# Install optional platform-specific dependencies (e.g. @rollup/rollup-linux-x64-gnu)
# in the lockfile so `npm ci` works on every OS, not just the one that generated
# the lockfile. See https://github.com/npm/cli/issues/4828.
include=optional
